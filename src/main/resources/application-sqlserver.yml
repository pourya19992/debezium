spring:
  datasource:
    url: jdbc:sqlserver://localhost:1433;databaseName=way;encrypt=false;trustServerCertificate=true;
    username: sa
    password: asd123!@#
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    database-platform: org.hibernate.dialect.SQLServerDialect
    hibernate:
      ddl-auto: update
    show-sql: true